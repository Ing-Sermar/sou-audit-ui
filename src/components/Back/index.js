import React from 'react';
import './style.scss';

const Back = () => {
  return (
    <div>
      <button className="voltar">Voltar</button>
      <button className="finalizar">Próximo</button>
    </div>
  );
};

export default Back;
